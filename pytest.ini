[pytest]
pythonpath = .
testpaths = tests
python_files = test_*.py
# Filtrar warnings específicos que no requieren acción:
# - Pandera 0.25.0: pandas/numpy removidos de dependencias base (usando pandera[polars])
# - Polars 1.32.0: deprecación de Categorical ordering físico (funcionalidad no afectada)
filterwarnings =
    ignore:Pandas and numpy have been removed from the base pandera:UserWarning:pandera
    ignore:the physical Categorical ordering is deprecated:DeprecationWarning:pandera.engines.polars_engine
